`pnpm add --filter ui <package_name>` for installing to ui folder
`pnpm add --filter server <package_name>` for installing to server folder
`pnpm add --filter crawl <package_name>` for installing to crawler folder